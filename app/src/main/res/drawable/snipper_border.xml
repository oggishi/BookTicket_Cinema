<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền của Spinner -->
    <solid android:color="#FFF3E0" /> <!-- Màu nền cam nhạt -->

    <!-- Thiết lập viền -->
    <stroke
        android:width="2dp"
        android:color="#000000" />

    <!-- Tùy chọn: bo tròn các góc -->
    <corners android:radius="8dp" />
</shape>
